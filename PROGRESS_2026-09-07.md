# Round 3 high-lambda extension — September 7, 2026

The four Round 4 failure-focused experiments completed. The latest request
is to train/evaluate Round 3 lambda=2.0 and lambda=5.0, changing only lambda
from the Round 3 lambda=1 setup. These are NOT failure-weighted or balanced.

Two fresh runs are submitted through `runs/round3-high-lambda/launch.py`.
Live state: `runs/round3-high-lambda/status.json`.
Live PID: `runs/round3-high-lambda/launcher.pid`.
Full commands/checkpoint paths: `runs/round3-high-lambda/COMMANDS.md`.

The same original Round 3 training/evaluation shell scripts are used directly.
Both train independently from untouched pretrained, 200 updates, physical
batch 32, 20 warmup steps, identical seed and data split, natural sampling and
unit query weights. Both training runs precede their paired 180-episode
evaluations. The W&B run initializes while waiting for its GPU start gate.
Do not duplicate active jobs or rerun finished Round 3/Round 4 evaluations.

The report merges the high-lambda status before Round 4 and places lambda=2
and lambda=5 immediately after Round 3 lambda=1, including while pending.
Report writes remain serialized and all previous completed results preserved.

http://127.0.0.1:8000/analysis_results/index.html#round3-move-alignment-comparison

The earlier overnight pause was resumed Sept 7 at 07:00 PDT; it is not an
active pause or a recurring timer. No new pause has been requested.

## Policy-output alignment correction

The high-lambda sweep completed before this work. User requested rescoring
Round 3 lambda=.1,.3,.5,1,2 using predicted translation sums rather than
realized end-effector displacement. Saved full 10x7 decoded policy chunks
allow exact rescoring of the same 180 episodes per checkpoint (and baseline),
without retraining or rerunning the simulator. Completed via:

```bash
source runs/round3/environment.sh
python -m experiments.robot.libero.policy_action_comparison
```

Outputs: `analysis_results/round3_policy_action_comparison.{json,html}`;
per-query scores and translation sums: `analysis_results/policy_action_rescoring/`.
New table: `index.html#round3-policy-action-alignment`. Original table preserved
byte-for-byte. All episodes, successes and query counts unchanged. Metric
uses the full predicted 10-action translation sum even for truncated execution;
stop cosine remains zero, epsilon 1e-8, mismatch c<.5.

Remaining distinction: training uses soft VQ decoding under teacher forcing;
rollout scores use decoded policy outputs. The displacement definition now
matches, but these prediction paths are not numerically identical. Lambda=5
and Round 4 were outside this five-run rescoring request. Ten targeted tests
passed, including opposite commanded/realized directions and full-chunk use.

## Expanded consistent-metric table

User requested a separate policy-action metric table directly below the
existing realized-displacement table. Rescoring now includes all matching
rows: pretrained, six Round 3 lambdas (.1,.3,.5,1,2,5), and all four Round 4
variants. The main report embeds the new table immediately after the original
table, with anchor `round3-policy-action-alignment`. Source generator remains
`experiments/robot/libero/policy_action_comparison.py`. Task-success counts
and original realized-displacement metrics remain unchanged.

## Latest display change: remove policy-action table

User requested removal of the new policy-action alignment table. Removed it
from index.html and the standalone Round 3 report, and removed the renderer
insertion code so report refreshes do not restore it. The original table
was verified unchanged. Saved rescoring artifacts are retained on disk.

## Six lambda=1 qualitative review figures

User requested six aligned, successful-rollout figures in the intro teaser
format, prioritizing Tasks 3 and 81. Generated six verified examples from
Round 3 lambda=1 saved videos: Task81/trial1/q15, Task4/trial0/q3,
Task17/trial1/q3, Task38/trial0/q22, Task50/trial1/q8, Task54/trial0/q2.
All selected episodes succeeded and realized-motion cosines range .894–.999.
Task 3 failed both saved trials, so Task4 front-butter is explicitly an
analogue, not a claimed Task3 recovery. Task81 matches task/subtask only.
The other four selected episodes had failed paired pretrained rollouts.

Images/PDFs/provenance: `analysis_results/figures/lambda1_qualitative/`.
Review at `index.html#lambda1-aligned-qualitative`, appended immediately
before closing main. Reproduction:
`python scripts/analysis/generate_lambda1_qualitative.py` after sourcing
`runs/round3/environment.sh`. Frames extracted from saved 224x224 videos,
indexed by cumulative executed chunk lengths; projected trajectory data
comes directly from the matching query log. Green dashed reasoning arrows
and blue realized-motion arrows use fixed display lengths for readability.
The blue trace uses actual logged projections. No generated/synthetic scenes.
Removed policy-action comparison table remains removed.


## Workstation restart save — 2026-09-07T14:43:41.009119-07:00

All requested training and evaluation complete; six qualitative figures saved at the end of the report. No training/evaluation processes active. See `HANDOFF_2026-09-07.md` for authoritative restart instructions and `runs/status_snapshot_2026-09-07.json` for state/checkpoint/provenance snapshot. Older overnight queue-resume instructions are superseded.


## Qualitative figure styling update

Regenerated all six lambda=1 qualitative PNG/PDF figures and contact sheet to match introduction figures: solid green (#16883f) reasoning arrows, solid red (#d62728) action arrows, linewidth 4 and identical arrowheads; red trajectory linewidth 3 and alpha 0.82. Updated HTML legend and provenance. Visually reviewed all six in contact sheet. Measurements and selected examples unchanged.


Restart save at 2026-09-07T16:52:59.088884-07:00: updated authoritative handoff and snapshot with final red/green figure styling; flushed local files to disk.
