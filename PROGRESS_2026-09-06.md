# Round 3 Move-alignment launch — 2026-09-06

Read `runs/round3/status.json` for current status; this note records the setup,
not a claim that the evaluations have finished. Do not duplicate active jobs.
The detached launcher PID is in `runs/round3/launcher.pid`.

## Authorized experiment

Three independent LoRA runs from the untouched MiniVLA checkpoint, lambda
0.1/0.3/0.5, normalized thresholded squared-hinge Move loss:
`mean((relu(0.5-cosine)/1.5)**2)`. Zero-vector/stop cosine handling is preserved.
All three runs use batch 32, 200 optimizer steps, 20 linear warmup steps and
then constant `1e-4`. All other Round 2 training settings and the split are
unchanged. The saved Round 2 artifact has 1,557 queries, despite the request's
1,577 figure; the exact artifact and the exact 1,245/312 split were reused.

## Files and commands

- Full commands, paths, step calculation and monitoring: `runs/round3/COMMANDS.md`.
- Train/evaluate scripts: `runs/round3/train.sh`, `runs/round3/evaluate.sh`.
- Persistent sequential queue: `runs/round3/launch.py`.
- Live machine-readable state: `runs/round3/status.json`.
- Training logs: `runs/round3/train-lambda{01,03,05}.log`.
- Evaluation logs: `runs/round3/eval-lambda{01,03,05}.log`.
- Report builder: `experiments/robot/libero/round3_move_comparison.py`.
- Report: `http://127.0.0.1:8000/analysis_results/index.html#round3-move-alignment-comparison`.
- Standalone HTML/JSON: `analysis_results/round3_move_alignment_comparison.{html,json}`.
- Source/dependency hashes: `runs/round3/source_manifest.json`.

All three train first, then all three evaluate with the identical Round 2
90-task, two-trial schedule (seed 20261201; six rollout processes). The prior
180-episode pretrained evaluation is reused. Completed checkpoint comparisons
must pass exact task/trial/seed checks before their results enter the report.
The report updates automatically at phase transitions and when W&B links
become available. The HTML preserves all earlier sections.

## Verification

- Boundary/negative/stop/finite-gradient loss tests passed.
- Full-model batch-32 backward probe passed: 57.956 GB peak allocation.
- Existing suite: 87 passed, 1 skipped; two additional Round 3 report tests passed.
- Round 2 train and validation ID lists match exactly.
- Query-weighted aggregation checked on unequal batches and live logs.
- Lambda 0.1 completed 200 optimizer steps; verified 6,385 total training
  queries, 20-step LR warmup, correct partial epoch, and native checkpoint.
- First W&B run: https://wandb.ai/yus047-/ecot-move-alignment/runs/36utml0u.
- Earlier HTML sections were hashed before launch and verified unchanged.
- Missing `/tmp/vq-bet` dependency restored using the prior reproduction command.

The launcher records failures per run and continues other independent runs.
It does not overwrite or restart failed outputs. Training scripts refuse
existing run directories. Consult status and logs before taking any recovery
action. No final rollout conclusions should be drawn from training/validation
metrics or incomplete evaluation outputs.

## Status at launch handoff

Lambda 0.1 and 0.3 finished training (200 updates each). Lambda 0.5 is
training. All 540 new evaluation episodes are queued and the detached
launcher remains active. This is a launch handoff, not final evaluation
results. Read the live status/report for subsequent progress.

## W&B runs

- lambda 0.1: https://wandb.ai/yus047-/ecot-move-alignment/runs/36utml0u
- lambda 0.3: https://wandb.ai/yus047-/ecot-move-alignment/runs/geob5kq5
- lambda 0.5: https://wandb.ai/yus047-/ecot-move-alignment/runs/xla4x26y

## Round 4 failure-focused launch

Two jobs are registered in `runs/round4/status.json`, with the persistent
controller PID in `runs/round4/launcher.pid`. They wait for the lambda=1
evaluation to finish before taking the GPU. W&B runs are live while waiting.
Full commands, checkpoint paths, exact sampler/loss semantics, validation
checks, and W&B URLs are in `runs/round4/COMMANDS.md`.

The weighted run uses only Move weights 1/3 with natural sampling. Balanced
sampling uses full 16/16 batches with unit weights. Both use the unchanged
Round 3 lambda=.5 configuration, 200 updates, and the exact training split.
Both train from the untouched checkpoint and automatically evaluate the same
180 paired episodes. Report builder merges the separate Round 3/4 states and
adds pending/completed rows to the existing table, preserving prior results.
Do not duplicate these jobs or manually create their GPU gate files.

### Round 4 lambda revision

User changed both queued failure-focused runs to lambda=1.0 before training.
The original lambda=.5 waiting processes were stopped and their registrations
archived in `runs/round4/superseded-lambda05`. Revised run names/checkpoints use
`round4-lambda1-failure-weight3` and `round4-lambda1-balanced-sampling`.
The new active controller remains in `runs/round4/launcher.pid`; original
Round 3 lambda=1 evaluation is unaffected. All other settings are unchanged.

### Latest Round 4 correction: lambda=.5

User reverted both failure-focused experiments to lambda=.5. Stopped the
lambda=1 weighted run at logged step 44 and balanced run before training.
Logs/state preserved in `runs/round4/superseded-lambda1`. New active runs
are `round4-lambda05-restart-failure-weight3` and
`round4-lambda05-restart-balanced-sampling`, each from the untouched checkpoint
for 200 fresh updates. Current status is `runs/round4/status.json`.

## Latest instruction: run all four overnight

User confirmed both methods at both lambda=.5 and lambda=1, and requested
overnight execution. See `OVERNIGHT_HANDOFF_2026-09-06.md` for the complete
saved handoff and both active queue paths. This supersedes earlier reversals.

## Latest: user paused jobs until Sept 7, 07:00 PDT

Both experiment process groups are suspended; a verified local systemd timer
will resume them at 7 AM. See `runs/overnight_pause.json` and the final section
of `OVERNIGHT_HANDOFF_2026-09-06.md`. Keep the computer powered on.
